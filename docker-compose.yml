name: cookie-consent-bundle

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cookie-consent-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
      - XDEBUG_MODE=coverage
    command: ["tail", "-f", "/dev/null"]
    networks:
      - cookie-consent-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  cookie-consent-bundle-network:
    driver: bridge
